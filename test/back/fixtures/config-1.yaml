# This Deployment runs our API component

root-dir-path: c:\tmp
open-browser : true
types:
  - name: "MARKDOWN_README"
    selectors:
      - equals: README.md
  - name: "DOCKER_COMPOSE"
    selectors:
      - starts-with: docker-compose
      - ends-with : yaml
      